"""Wi-Fi Detection demo (Ryan)."""
import streamlit as st

st.title("Wi-Fi Detection")
st.caption("Owner: Ryan · backend/app/services/wifi_detection/")

st.info("Skeleton page. Wire up to `/api/wifi-detection/...` when the service ships.")

st.markdown(
    """
    ### Planned demo

    - Replay a recorded CSI stream (no live hardware needed)
    - Show amplitude/phase plots
    - Highlight detected motion / fall windows
    """
)
