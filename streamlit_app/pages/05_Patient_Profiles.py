"""Patient Profiles demo (Affan)."""
import streamlit as st

st.title("Patient Profiles")
st.caption("Owner: Affan · data/synthetic/ + data/preprocessing/")

st.info("Skeleton page. Hook up once synthetic profiles are generated.")

st.markdown(
    """
    ### Planned demo

    - Browse generated synthetic patient profiles
    - Show fields: demographics, medical history, medications, functional status,
      fall history, emergency contacts, risk level (low / moderate / high)
    - Simple filters + summary stats over the cohort
    """
)
