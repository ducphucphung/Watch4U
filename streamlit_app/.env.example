# Copy to .env and fill in real values

# Where the FastAPI backend lives — inside docker-compose use the service name
BACKEND_URL=http://backend:8000

# When running Streamlit locally outside Docker, use:
# BACKEND_URL=http://localhost:8000
